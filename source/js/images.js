/*  eslint no-var: "error"  */
/*  eslint-env es6  */

'use strict';

(function () {

  Modernizr.on('webp', function (result) {
    if (result) {
      // supported
    } else {
      // not-supported
    }
  });

})();
